generator.id=${cloudhopper.generator.id}
provider.name=${cloudhopper.provider.name}
provider.version=${cloudhopper.provider.version}
deployment.tool=${cloudhopper.deployment.tool}
deployment.version=${cloudhopper.deployment.version}
java.version=${cloudhopper.java.version}
native.support=${cloudhopper.native.support}
description=${cloudhopper.description}
integration.strategy=${cloudhopper.integration.strategy}

# Maven Coordinates
maven.groupId=${project.groupId}
maven.artifactId=${project.artifactId}
maven.version=${project.version}